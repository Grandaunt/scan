<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bcm.havoc.wmapp_20181108">

    <!-- 读取设置？？？ -->
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />

    <!-- 拨打电话 -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- 拍照权限  一般拍照和摄像的时候需要写到sd卡上，所以还有一向权限声明如下 -->

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 获取地理位置 -->
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 不是SDK需要的权限，是示例中的后台唤醒定位需要的权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 锁屏弹窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- 语音合成 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- OpenGLES 2.0 -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <!-- 屏幕自适应 -->
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="false"
        android:resizeable="true"
        android:smallScreens="true" />

    <!-- OpenGLES 2.0 -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan|stateHidden"
        tools:replace="icon,theme">
        <activity
            android:name=".UI.BG.TTSActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".ui.LoadingActivity" /> -->
        <activity android:name=".UI.LoginActivity" />
        <activity android:name=".UI.RK.RkListActivity" />
        <activity android:name=".UI.CK.CkListActivity" />
        <activity android:name=".UI.PD.PdListActivity" />
        <activity android:name=".UI.GOODS.GoodsActivity" />
        <activity android:name=".UI.GOODS.GTJActivity" />
        <activity android:name=".UI.YK.YkListActivity" />
        <activity android:name=".UI.RK.RkActivity" />
        <activity android:name=".UI.CK.CkActivity" />
        <activity android:name=".UI.Menu.AboutActivity" />
        <activity android:name=".UI.PD.PdActivity" />
        <!-- <activity android:name=".Base.ui.SeekbarActivity" /> -->
        <!-- <activity -->
        <!-- android:name=".ManagerTmActivity" -->
        <!-- android:label="@string/title_activity_manager_tm" /> -->
        <activity android:name=".UI.Menu.SettingActivity" />
        <!-- <activity android:name=".UI.ProcurementPlanActivity" /> -->
        <!-- <activity android:name=".UI.DeadStockActivity" /> -->
        <activity android:name=".UI.Menu.ScanActivity" />
        <activity android:name=".UI.Menu.SupplierActivity" />
        <!-- <activity android:name=".UI.SearchResultActivity"></activity> -->
        <!-- <activity -->
        <!-- android:name=".ui.SearchResultActivity" -->
        <!-- android:parentActivityName=".ui.MainActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".ui.MainActivity" /> -->
        <!-- &lt;!&ndash; meta tag and intent filter go into results activity &ndash;&gt; -->
        <!-- <meta-data -->
        <!-- android:name="android.app.searchable" -->
        <!-- android:resource="@xml/searchable" /> -->


        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.SEARCH" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity android:name=".UI.Express.ExpressActivity" />
        <activity android:name=".UI.Menu.CustomerActivity" />
        <activity android:name=".UI.Menu.InventoryActivity" />
        <activity android:name=".UI.Menu.FeedbackActivity" />
        <activity android:name=".UI.Menu.InstructionsActivity" />
        <activity android:name=".UI.RK.RKGoodInfoActivity" />
        <activity android:name=".UI.GOODS.ItemInfoActivity" />
        <activity android:name=".UI.Menu.RKBBActivity" />
        <activity android:name=".UI.Menu.CKBBActivity" />
        <!-- <activity -->
        <!-- android:name=".Base.UI.SettingsActivity" -->
        <!-- android:label="@string/title_activity_settings" /> -->
        <activity android:name=".UI.BG.BGInActivity"/>

             <activity android:name=".UI.BG.BGOutActivity">

        <!--<activity android:name=".UI.BG.TTSActivity">-->

        </activity>
    </application>

</manifest>